#set($items = [])
#foreach($item in $context.result.items)
	$util.qr($items.add({ "replyID": $item.commentID, "datetime": $item.datetime, "user": { "userID": $item.userID }, "content": $item.content }))
#end
{
    "items": $util.toJson($items),
    "nextToken": $util.toJson($util.defaultIfNullOrEmpty($context.result.nextToken, null))
}
